import { HandleErrorDirective } from './handle-error.directive';

describe('HandleErrorDirective', () => {
  it('should create an instance', () => {
    const directive = new HandleErrorDirective();
    expect(directive).toBeTruthy();
  });
});
